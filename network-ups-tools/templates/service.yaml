apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-ups-tools.fullname" . }}
  labels:
    {{- include "network-ups-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "network-ups-tools.selectorLabels" . | nindent 4 }}
